# IP-only access (no domain, no HTTPS)
:80 {
    reverse_proxy localhost:8000
    encode gzip
}

# If you add a domain later, replace the above with:
# tracker.yourdomain.com {
#     reverse_proxy localhost:8000
#     encode gzip
# }
